import SidebarLayout from './layout/SidebarLayout';

const SubAdmin = ({children}) => {
    return (
        <SidebarLayout>
            {children}
        </SidebarLayout>
    )
}

export default SubAdmin
